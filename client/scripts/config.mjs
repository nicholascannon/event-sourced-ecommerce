export const INDEX_HTML_PATH = './src/index.html';
export const APP_ENTRYPOINT = './src/index.tsx';
export const OUTPUT_DIR = './dist';
// You need to add this script tag to your index.html
// It's also used to serve the app locally (see serve.mjs).
export const INDEX_HTML_SCRIPT_TAG = '<script src="js/index.js"></script>';
export const SERVE_PORT = 3000;
export const SERVE_DIR = './src'; // This should be the dir where your index.html lives
