import React from 'react';
import { Button } from '@mui/material';

export function App() {
    return (
        <main>
            <Button variant="contained">click me</Button>
        </main>
    );
}
