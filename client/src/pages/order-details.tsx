import React from 'react';
import { Typography } from '@mui/material';

export const OrderDetailsPage = () => {
    return (
        <>
            <Typography variant="h4">Order #X</Typography>
        </>
    );
};
